---
---
<div class="overflow-hidden bg-gold py-3 border-y-0">
  <div class="marquee-track">
    {[...Array(2)].map(() => (
      <div class="flex items-center gap-0 shrink-0">
        {['1,000+ Units Delivered','Owner on Every Project','Houston Metro','48hr Bid Turnaround','Licensed & Insured','Multifamily Only','Fixed-Price Contracts','Full Gut Rehabs','Unit Renovations','Amenity Upgrades'].map((item) => (
          <span class="text-white text-xs font-bold uppercase tracking-[0.2em] px-8 border-r border-white/30 whitespace-nowrap">{item}</span>
        ))}
      </div>
    ))}
  </div>
</div>
